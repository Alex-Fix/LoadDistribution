namespace LoadDistribution.Core.Domain.Enums;

public enum DependencyType
{
      Undefined = 0,
      Absolute = 10,
      StudentCount = 20,
      GroupCount = 30,
      SubgroupCount = 40,
      ThreadCount = 50
}
